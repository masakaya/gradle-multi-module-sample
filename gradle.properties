# GitHub Packages credentials (for local publishing)
# Set these environment variables or add to ~/.gradle/gradle.properties
# gpr.user=your-github-username
# gpr.key=your-github-personal-access-token

# Database configuration
db.url=jdbc:mysql://localhost:3306/mydb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=dbuser
db.password=dbpassword